# Get connection list

Extract the connection (block pairing) structure.

## Usage

``` r
get_connection(x, ...)
```

## Arguments

- x:

  An `mrf3_fit` or `mrf3` object.

- ...:

  Additional arguments (unused).

## Value

A list of character vectors (each a block pair), or `NULL`.
