% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrf3_workflow_simple.R
\name{mrf3}
\alias{mrf3}
\title{Simplified mrf3 Entry Point}
\usage{
mrf3(
  dat.list,
  k = NULL,
  ntree = 500,
  top_v = NULL,
  samptype = c("swor", "swr"),
  main_clustering = c("similarity", "proximity", "enhanced_proximity"),
  filter_mode = c("none", "auto", "manual"),
  seed = 529,
  ...
)
}
\arguments{
\item{dat.list}{A named list of omics matrices (samples in rows, features in columns).}

\item{k}{Optional cluster number used for both shared and specific clustering.
If \code{NULL}, \code{k} is tuned in downstream clustering routines.}

\item{ntree}{Number of trees for RF fitting.}

\item{top_v}{Optional unified top-v cutoff.
If set, applies to both \code{model_top_v} and \code{fused_top_v}.}

\item{samptype}{Sampling scheme passed through to \code{mrf3_fit()}.}

\item{main_clustering}{Global clustering strategy applied consistently to
shared, specific, and robust branches: \code{"similarity"} (default),
\code{"proximity"}, or \code{"enhanced_proximity"}.}

\item{filter_mode}{Filtering mode passed to \code{mrf3_fit()}.}

\item{seed}{Random seed.}

\item{...}{Advanced options passed to \code{mrf3_fit()}, e.g.
\code{model_top_v_tune_args}, \code{fused_top_v_tune_args},
\code{clustering_args}, \code{run_imd}, \code{imd_args},
\code{run_variable_selection}, \code{variable_selection_args},
\code{cluster_imd_args}, \code{run_robust_clustering}, \code{compact_output}.}
}
\value{
An object of class \code{"mrf3_fit"}.
}
\description{
A compact wrapper around \code{mrf3_fit()} for common use cases.
It keeps only the most frequently used parameters and forwards advanced
options through \code{...}.
By default, this wrapper runs shared/specific weighting and specific
clustering branches.
}
