% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_mrf3_fit.R
\name{plot.mrf3_fit}
\alias{plot.mrf3_fit}
\title{Plot an \code{mrf3_fit} object}
\usage{
\method{plot}{mrf3_fit}(
  x,
  type = c("tsne", "umap", "embed", "network", "circos", "composition", "km", "weights"),
  ...,
  annotation = NULL,
  time_var = NULL,
  event_var = NULL,
  pheno_mat = NULL
)
}
\arguments{
\item{x}{An \code{mrf3_fit} object returned by \code{\link[=mrf3_fit]{mrf3_fit()}} (or \code{\link[=mrf3]{mrf3()}}).}

\item{type}{Plot type; one of \code{"tsne"}, \code{"umap"}, \code{"embed"}, \code{"network"},
\code{"circos"}, \code{"composition"}, \code{"km"}, \code{"weights"}. Defaults to \code{"tsne"}.}

\item{...}{Additional arguments forwarded to the target plot function
(e.g. \code{seed}, \code{perplexity}, \code{cutoff}, \code{top}, \code{source}, \code{omics},
\code{group}, \code{cut.off}, \code{risk.table}). For \code{type = "circos"},
\code{feature_source} and \code{normalized} are forwarded to \code{\link[=pairwise_imd]{pairwise_imd()}}.
For \code{type = "embed"}, \code{embed_dims} sets the number of principal
components displayed.}

\item{annotation}{Required for \code{type = "composition"}: a vector or factor
of external sample annotations (e.g. tumour subtypes), one value per
sample and in the same sample order as the fitted data. The fit itself
stores no annotations.}

\item{time_var}{Required for \code{type = "km"}: name of the survival/follow-up
time column in \code{pheno_mat}.}

\item{event_var}{Required for \code{type = "km"}: name of the binary 0/1 event
column in \code{pheno_mat}.}

\item{pheno_mat}{Required for \code{type = "km"}: data frame of sample-level
phenotype data containing \code{time_var} and \code{event_var}, with one row per
sample in the same sample order as the fitted data.}
}
\value{
Whatever the target plot function returns: a \code{ggplot} object for
\code{"tsne"}, \code{"umap"}, \code{"composition"}, and \code{"weights"}; a \code{ggsurvplot}
object for \code{"km"}; invisibly, an \code{igraph} object for \code{"network"}, the
chord-diagram data for \code{"circos"}, and \code{NULL} for \code{"embed"}.
}
\description{
Unified plotting entry point for fitted multiRF pipelines. The method
extracts the components each display needs from the fit (the learned
sample similarity, cluster labels via \code{\link[=get_clusters]{get_clusters()}}, IMD weights via
\code{\link[=get_weights]{get_weights()}}, pairwise feature adjacency via \code{\link[=pairwise_imd]{pairwise_imd()}}) and
forwards them, together with \code{...}, to the corresponding exported plot
function, so every option of the underlying function stays reachable.
}
\details{
Supported \code{type} values and their targets:
\itemize{
\item \code{"tsne"} (default): \code{\link[=plot_tsne]{plot_tsne()}} on the fitted similarity matrix
(t-SNE), coloured by shared cluster.
\item \code{"umap"}: \code{\link[=plot_umap]{plot_umap()}} on the fitted similarity matrix.
\item \code{"embed"}: deprecated pairs display via \code{\link[=plot_embed]{plot_embed()}}, drawn on the top
principal components of the fitted similarity matrix (control the number
of components with \code{embed_dims}, default 3).
\item \code{"network"}: \code{\link[=plot_network]{plot_network()}} sample network from the fitted similarity
matrix, vertices coloured by shared cluster.
\item \code{"circos"}: \code{\link[=pairwise_imd]{pairwise_imd()}} on the fit, then \code{\link[=plot_circos]{plot_circos()}} of the
feature-level adjacency (\code{source = "adj_var"} by default). Requires IMD
(\code{run_imd = TRUE}) and retained models (\code{compact_output = FALSE}).
\item \code{"composition"}: \code{\link[=plot_cluster_composition]{plot_cluster_composition()}} of shared clusters against
a user-supplied \code{annotation} vector.
\item \code{"km"}: \code{\link[=plot_km]{plot_km()}} Kaplan--Meier curves of the shared clusters; the fit
holds no survival data, so \code{time_var}, \code{event_var}, and \code{pheno_mat} must
be supplied.
\item \code{"weights"}: \code{\link[=plot_weights]{plot_weights()}} of the per-block IMD weights. Requires
\code{run_imd = TRUE} (or cluster-level IMD via \code{run_cluster_imd = TRUE}).
}

For every type the default sample grouping is \code{get_clusters(x)}; pass
\code{group} through \code{...} to override it (for \code{"tsne"}, \code{"umap"}, \code{"embed"},
\code{"network"}, \code{"composition"}, and \code{"km"}). When \code{source = "robust_clustering"} is passed through \code{...}, robust cluster labels are
used instead, and \code{source = "specific_specific"} with \code{omics} uses that
block's specific cluster labels.

Cluster colours are consistent across all types: cluster ids are mapped in
sorted order onto a fixed colour-blind-safe palette (the same palette the
individual plot functions use), so cluster 2 has the same colour in the
t-SNE, network, and Kaplan--Meier displays. A user-supplied colour setting
passed through \code{...} (for example \code{palette} for \code{"km"} or \code{colours} for
\code{"composition"}) always wins over this default, and the returned ggplot
objects accept a replacement \code{scale_fill_manual()} /
\code{scale_colour_manual()}.
}
\examples{
\donttest{
data("tcga_brca_data", package = "multiRF")
ids <- rownames(tcga_brca[[1]])[seq_len(80)]
dat <- lapply(tcga_brca, function(x) {
  x[ids, seq_len(min(30L, ncol(x))), drop = FALSE]
})

fit <- mrf3_fit(
  dat,
  ntree = 30,
  filter_mode = "none",
  clustering_args = list(shared_k = 3),
  run_imd = TRUE,
  return_data = TRUE,
  filter_verbose = FALSE,
  verbose = FALSE,
  seed = 529
)

## Embeddings and networks of the learned similarity
plot(fit, type = "tsne", seed = 529)
plot(fit, type = "network", seed = 529)

## IMD feature weights
plot(fit, type = "weights", top = 8)

## Cluster composition against an external annotation
annotation <- factor(rep(c("Subtype A", "Subtype B"), length.out = 80))
plot(fit, type = "composition", annotation = annotation)

## Kaplan-Meier curves need user-supplied survival data
if (requireNamespace("survival", quietly = TRUE) &&
    requireNamespace("survminer", quietly = TRUE)) {
  pheno <- data.frame(
    os_time = stats::rexp(80, rate = 1 / 50),
    os_event = stats::rbinom(80, 1, 0.6)
  )
  plot(fit, type = "km", time_var = "os_time", event_var = "os_event",
       pheno_mat = pheno, risk.table = FALSE)
}
}

}
\seealso{
\code{\link[=plot_tsne]{plot_tsne()}}, \code{\link[=plot_umap]{plot_umap()}}, \code{\link[=plot_network]{plot_network()}}, \code{\link[=plot_circos]{plot_circos()}},
\code{\link[=plot_cluster_composition]{plot_cluster_composition()}}, \code{\link[=plot_km]{plot_km()}}, \code{\link[=plot_weights]{plot_weights()}},
\code{\link[=get_clusters]{get_clusters()}}, \code{\link[=pairwise_imd]{pairwise_imd()}}
}
