% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_sub_mrf.R
\name{fit_sub_multi_rfsrc}
\alias{fit_sub_multi_rfsrc}
\title{Fit sub-MRF ensemble for a multi-omics connection list}
\usage{
fit_sub_multi_rfsrc(
  dat.list,
  connect_list,
  n_sub = 15L,
  frac_response = 0.2,
  frac_predictor = 0.2,
  ntree_per_sub = 25L,
  mtry = NULL,
  ytry = NULL,
  min_response = 20L,
  min_predictor = 30L,
  min_response_for_sub = 500L,
  min_predictor_for_sub = 500L,
  ntree_full = 300L,
  enhanced = FALSE,
  compute_imd = FALSE,
  imd_args = list(),
  seed = 529L,
  parallel = FALSE,
  cores = 2L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{dat.list}{Named list of data frames (samples x features).}

\item{connect_list}{A list of connections, each a character vector of
length 2: \code{c(response_name, predictor_name)}.  Same format as used
by \code{fit_multi_forest()}.}

\item{n_sub}{Integer; number of sub-MRF replicates to fit.}

\item{frac_response}{Fraction of response columns to sample per replicate
(default 0.1).  Convergence analysis shows symmetric low fractions
(0.1--0.3 for both response and predictor) yield the best
approximation of the full forest-weight matrix.}

\item{frac_predictor}{Fraction of predictor columns to sample per replicate
(default 0.1).  Avoid setting this to 1.0 when \code{frac_response} is
small, as the asymmetric case leads to poor convergence (each
sub-model overfits to its response subset).}

\item{ntree_per_sub}{Number of trees per sub-MRF (default 50).}

\item{mtry}{Number of candidate X variables per split. Passed through to
\code{fit_forest()}.}

\item{ytry}{Number of candidate Y variables per split
(0 = \code{min(qy, ceiling(p/3))} default).}

\item{min_response}{Minimum number of response columns per sub-MRF.}

\item{min_predictor}{Minimum number of predictor columns per sub-MRF.}

\item{min_response_for_sub}{Minimum response-block size below which all
response features are used instead of sub-sampling.}

\item{min_predictor_for_sub}{Minimum predictor-block size below which all
predictor features are used instead of sub-sampling.}

\item{ntree_full}{Deprecated compatibility argument. Ignored.}

\item{enhanced}{Logical; if \code{TRUE}, compute soft enhanced proximity
using full-data sample embeddings within each sub-model.  The result
is stored as \verb{$enhanced_prox} in the output so that
\code{mrf3_cl_prox()} can use it directly without re-traversing trees.
Default \code{FALSE} because the extra computation is non-trivial.}

\item{compute_imd}{Logical; whether to pre-compute IMD weights across sub-models.}

\item{imd_args}{Named list of arguments passed to \code{get_imp_forest()} when
\code{compute_imd = TRUE}.}

\item{seed}{Base random seed.}

\item{parallel}{Logical; if \code{TRUE}, use \code{\link[parallel:mclapply]{parallel::mclapply()}}.}

\item{cores}{Number of cores when \code{parallel = TRUE}.}

\item{verbose}{Logical; print progress messages.}

\item{...}{Passed to \code{fit_sub_mrf()} and then \code{fit_forest()}.}
}
\value{
A \strong{named list} of rfsrc-compatible objects, one per connection.
Names follow the \code{"response_predictor"} convention used by
\code{fit_multi_forest()}.  Each object contains \code{forest.wt}, \code{forest.wt.oob},
\code{proximity}, \code{xvar}, \code{yvar}.
}
\description{
Higher-level wrapper that matches the interface of \code{fit_multi_forest()}.
For each directed connection (response_block -> predictor_block),
features are sub-sampled from both blocks independently, and the
resulting n x n matrices are averaged across replicates.
}
\details{
Sub-MRF uses the default engine (native C++) for its internal fits.
OOB forest-weight reconstruction uses the \code{inbag} matrix returned by
the native engine.
}
