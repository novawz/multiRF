% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrf3_vs.R
\name{mrf3_vs}
\alias{mrf3_vs}
\title{MRF variable selection}
\usage{
mrf3_vs(
  mod,
  dat.list = NULL,
  method = "filter",
  signal = c("shared", "specific", "all"),
  se = 1,
  c1 = "normal",
  c2 = "normal",
  level = 0.05,
  tscore = FALSE,
  use_distribution = TRUE,
  re_weights = FALSE,
  re_fit = TRUE,
  ntree = NULL,
  scale = FALSE,
  k = 3,
  tau_grid = seq(0.8, 3.1, by = 0.1),
  tol = 0.01,
  perm_B = 20L,
  perm_quantile = 0.95,
  iter = 1000,
  eps = 1e-05,
  normalized = FALSE,
  select = "ALL",
  ...
)
}
\arguments{
\item{mod}{A fitted \code{mrf3_fit} object, or an \code{mrf3}-like object that already
contains raw forest IMD in \code{mod$imd}.}

\item{dat.list}{A named list of omics matrices used for feature selection
and optional refitting.}

\item{method}{Feature-selection rule. \code{"filter"} adaptively tunes the
cutoff \code{tau * sd(IMD)} using OOB error: for the shared signal the
cross-omics forests are refitted at each candidate cutoff and scored by
OOB normalized prediction error across predictor and response coordinates,
and the cutoff is the first stable plateau of that error; for the specific signal there is no
second block to predict, so by default the cutoff is the
\code{perm_quantile} quantile of a permutation null: the residual forest is
refitted \code{perm_B} times on column-wise permuted residuals (no
between-variable structure, same marginals) and the pooled IMD of those
refits defines the importance reachable without structure
(an OOB search is not used for the specific signal because the OOB
error of a residual block barely changes with the cutoff). The specific
branch requires the residual matrices kept by \code{mrf3_fit()};
\code{"mixture"} fits a point-mass/two-component model; and \code{"test"} (alias
\code{"transformation"}) selects by a forest-level t-score: each
feature's forest IMD is standardized against the mean forest IMD of
all features in the block, using the feature's across-tree standard
error, and features in the upper tail of a Student-t reference with
\code{ntree - 1} degrees of freedom (\code{p < level}) are kept; see Details.
\code{"thres"} applies a fixed \code{se * sd(IMD)} cutoff.}

\item{signal}{Which signal component to select on: \code{"shared"} (cross-modal),
\code{"specific"} (per-block residual), or \code{"all"} (the union of both).}

\item{se}{The fixed value of tau used by \code{method = "thres"}.}

\item{c1}{Distribution family for the lower-IMD (noise) component in
mixture mode: \code{"normal"} or \code{"truncn"}.}

\item{c2}{Distribution family for the higher-IMD component in mixture mode:
\code{"normal"} or \code{"gamma"}.}

\item{level}{One-sided significance level for the transformation, or the
maximum posterior noise probability for mixture selection. A single
numeric value is applied to every block; a named vector/list supplies
block-specific values. \code{"auto"} retains the optional Bayesian-FDR rule in
mixture mode.}

\item{tscore}{Deprecated compatibility switch. Use
\code{method = "transformation"} instead.}

\item{use_distribution}{Deprecated compatibility argument.}

\item{re_weights}{Logical; whether selected IMD values are supplied as
variable-sampling weights during the final refit.}

\item{re_fit}{Logical; whether to refit forests after feature selection.}

\item{ntree}{Number of trees used in the optional final refit. \code{NULL} reuses
the fitted object's tree count, falling back to 300.}

\item{scale}{Logical; whether to standardize selected data before refitting.}

\item{k}{Number of repeated forest fits at each candidate filtering cutoff.}

\item{tau_grid}{Candidate multipliers of \code{sd(IMD)} evaluated by
\code{method = "filter"}. Default \code{seq(0.8, 3.1, by = 0.1)}.}

\item{tol}{Tolerable adjacent change in mean OOB normalized MSE used to
choose the filtering cutoff.}

\item{perm_B}{Number of permutation refits per residual block used by
\code{method = "filter"} for the specific signal.}

\item{perm_quantile}{Quantile of the pooled permutation-null IMD used as
the specific-signal cutoff.}

\item{iter}{Maximum EM iterations in mixture mode.}

\item{eps}{EM convergence tolerance in mixture mode.}

\item{normalized}{Logical; whether to L2-normalize the retained weights
after selection. These methods are always evaluated on raw IMD;
therefore the default is \code{FALSE}.}

\item{select}{Data block(s) to which selection is applied; default \code{"ALL"}.}

\item{...}{Additional arguments passed to forest fitting.}
}
\value{
An object inheriting from \code{mrf3} and \code{vs}. For \code{signal = "all"},
\code{dat.list} and \code{imd} contain the union selection and the two component
results are retained in \code{signal_results}.
}
\description{
MRF variable selection
}
\details{
For \code{method = "transformation"}, every connected forest supplies a
per-tree IMD matrix (features x trees) per block. Feature \code{v} is
standardized as \code{t_v = (M_v - mu) / SE(M_v)}, where \code{M_v} is the feature's forest IMD
(its per-tree IMD averaged over the \code{B} trees), \code{mu} is the mean forest
IMD over all features of the block, and \code{SE(M_v)} is the feature's
across-tree standard error (\verb{sd(per-tree IMD) / sqrt(B)}). Features
with an upper-tail \verb{p = P(T_\{B-1\} > t_v) < level} are selected, i.e.
features whose forest IMD lies significantly above the block-wide
mean. When several connected forests cover the same block, each forest
is evaluated separately and a feature is retained by strict majority
vote across forests; the reported t-scores and p-values are averaged.
Because per-tree IMDs within a forest are correlated (trees share the
training data) and \code{mu} is treated as a fixed reference, the p-values
are screening scores rather than exact test levels.
}
