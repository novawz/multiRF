% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tSNE.R
\name{plot_tsne}
\alias{plot_tsne}
\alias{plot_network}
\alias{plot_weights}
\alias{plot_circos}
\alias{plot_embed}
\alias{plot_umap}
\title{Multiple plot functions}
\usage{
plot_tsne(
  dat = NULL,
  mod = NULL,
  group = NULL,
  label_group = TRUE,
  position = "right",
  perplexity = 30,
  pca = FALSE,
  ncomp = 70,
  main = "t-SNE",
  size = 1.4,
  shape = 21,
  source = "auto",
  omics = NULL,
  cluster = NULL,
  seed = NULL,
  base_size = 8,
  base_family = "sans",
  ...
)

plot_network(
  dat,
  group = NULL,
  label = FALSE,
  cutoff = NULL,
  layout = igraph::layout_with_fr,
  vertex.size = 5,
  label.dist = NULL,
  edge.width = NULL,
  vertex.frame.color = "white",
  vertex.label.color = "black",
  vertex.label.cex = 0.5,
  vertex.label.dist = 0,
  edge.curved = 0.5,
  vertex.label.degree = -pi/2,
  position = "bottomright",
  source = "auto",
  omics = NULL,
  cluster = NULL,
  seed = NULL,
  ...
)

plot_weights(
  weights,
  plot.which = "all",
  top = 20,
  labels = NULL,
  weight_source = "auto",
  cluster = NULL,
  ncol = NULL,
  base_size = 8,
  base_family = "sans"
)

plot_circos(
  mat,
  names.list = NULL,
  group = NULL,
  cut.off = NULL,
  highlight = NULL,
  source = c("adj_var", "adj_dat"),
  ...
)

plot_embed(dat, group = NULL, position = "bottom", pch = 20, ...)

plot_umap(
  dat,
  group = NULL,
  main = "UMAP",
  label_group = TRUE,
  pca = TRUE,
  ncomp = 70,
  position = "right",
  pch = 21,
  size = 1.4,
  config = umap::umap.defaults,
  method = "naive",
  source = "auto",
  omics = NULL,
  cluster = NULL,
  seed = NULL,
  base_size = 8,
  base_family = "sans",
  ...
)
}
\arguments{
\item{dat}{A data frame or matrix}

\item{mod}{A fitted model object used to compute embeddings internally.
Can also be an \code{mrf3_fit} object.}

\item{group}{Class group}

\item{label_group}{Logical; whether to draw group labels on embeddings.}

\item{position}{Legend position.}

\item{perplexity}{tSNE perplexity used by \code{Rtsne::Rtsne()}.}

\item{pca}{Logical; whether to run PCA before embedding.}

\item{ncomp}{Number of principal components retained when \code{pca = TRUE}.}

\item{main}{a main title for the plot, see also \code{\link[graphics]{title}}.}

\item{size}{Point size in scatter plots.}

\item{shape}{Point shape in embedding plots.}

\item{source}{Source used when \code{dat}/\code{mod}/\code{weights} is an \code{mrf3_fit}.
Supported values for \code{mrf3_fit} inputs:
\code{"auto"}, \code{"specific_shared"}, \code{"specific_specific"}, \code{"clustering"},
\code{"robust_clustering"}, \code{"reconstruction_weight_all"},
\code{"reconstruction_weight_block"}, \code{"reconstruction_fused_block"}.
\code{plot_network()} and \code{plot_circos()} additionally accept a
\code{pairwise_imd_analysis} object with \code{source = "adj_var"} or \code{"adj_dat"}.
The native t-SNE model path requires a square sample-similarity source;
use the \code{dat} argument for a raw rectangular feature matrix.
For \code{plot_weights()}, supported values are:
\code{"auto"}, \code{"imd"}, \code{"cluster_imd"}, \code{"mrf"}.}

\item{omics}{Optional omics block name when \code{source} is block-specific.}

\item{cluster}{Optional cluster label when \code{source} uses cluster-level IMD output.}

\item{seed}{Optional non-negative integer seed for stochastic embedding or
network-layout initialization. The caller's RNG state is restored.}

\item{base_size}{Base font size used by the multiRF plotting theme.}

\item{base_family}{Font family used by the multiRF plotting theme.}

\item{...}{Additional arguments passed to the underlying plotting routine.}

\item{label}{A logical parameter that determine whether to show labels or not.}

\item{cutoff}{Edge-weight cutoff. For a regular sample matrix the default
is the mean positive upper-triangle weight; for pairwise IMD it is the 75th
percentile of positive upper-triangle weights.}

\item{layout}{Network layout function for graph plotting.}

\item{vertex.size}{Vertex size in network plots.}

\item{label.dist}{Label distance in network plots.}

\item{edge.width}{Edge width in network plots.}

\item{vertex.frame.color}{Vertex frame color in network plots.}

\item{vertex.label.color}{Vertex label color in network plots.}

\item{vertex.label.cex}{Vertex label font size in network plots.}

\item{vertex.label.dist}{Vertex label distance in network plots.}

\item{edge.curved}{Edge curvature in network plots.}

\item{vertex.label.degree}{Vertex label angle in network plots.}

\item{weights}{Weight vector or weight-carrying object for \code{plot_weights()}.}

\item{plot.which}{Which omics block(s) to plot in \code{plot_weights()}.}

\item{top}{The number of top weighted variables to show in the plot. The default is 20. Can be chosen from numerical values or all for setting the parameter = NULL.}

\item{labels}{Optional panel labels in \code{plot_weights()}.}

\item{weight_source}{Alias of \code{source} used by \code{plot_weights()}.}

\item{ncol}{Number of facet columns in \code{plot_weights()}. By default, plots
with more than two blocks use two columns to preserve readable feature
labels.}

\item{mat}{Adjacency-like matrix for \code{plot_circos()}.}

\item{names.list}{Named list of variable groups for \code{plot_circos()}.}

\item{cut.off}{Cutoff applied before plotting a circos graph.}

\item{highlight}{Vector of sectors to highlight in \code{plot_circos()}.}

\item{pch}{Point symbol for base plotting functions.}

\item{config}{UMAP configuration object passed to \code{umap::umap()}.}

\item{method}{UMAP backend method passed to \code{umap::umap()}.}
}
\value{
\code{plot_tsne()}, \code{plot_umap()}, and \code{plot_weights()} return a
\code{ggplot} object. \code{plot_network()} invisibly returns an \code{igraph} object,
\code{plot_circos()} invisibly returns the chord-diagram result, and the
deprecated \code{plot_embed()} invisibly returns \code{NULL} after drawing.
}
\description{
Multiple plot functions
}
