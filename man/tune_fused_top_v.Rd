% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_t.R
\name{tune_fused_top_v}
\alias{tune_fused_top_v}
\title{Tune fused weight top-v truncation}
\usage{
tune_fused_top_v(
  dat.list,
  mod,
  vmin = 10,
  by = 1,
  vmax = NULL,
  model_top_v = 10,
  k = NULL,
  sample_n = NULL,
  sample_frac = NULL,
  auto_sample_n = TRUE,
  max_candidates = 20,
  reuse_tuned_k = TRUE,
  parallel = TRUE,
  cores = NULL,
  seed = 529,
  object = "entropy_elbow",
  early_stop = FALSE,
  elbow_rel_tol = 0.25,
  elbow_abs_tol = 1e-04,
  elbow_min_points = 4L,
  elbow_min_frac = 0.2,
  elbow_patience = 2L,
  elbow_smooth_window = 3L
)
}
\arguments{
\item{dat.list}{A list of omics matrices used for clustering.}

\item{mod}{A fitted \code{mrf3} model object.}

\item{vmin}{Minimum \code{fused_top_v} cutoff to evaluate.}

\item{by}{Base step size for the \code{fused_top_v} grid.}

\item{vmax}{Maximum \code{fused_top_v} cutoff to evaluate.
If \code{NULL}, an adaptive small-v upper bound is used.}

\item{model_top_v}{Fixed model-level top-v cutoff used while tuning
\code{fused_top_v}.}

\item{k}{Optional fixed number of clusters.}

\item{sample_n}{Optional integer. If set, tune on a random subset of samples.}

\item{sample_frac}{Optional fraction in (0, 1]. Used when \code{sample_n} is \code{NULL}.}

\item{auto_sample_n}{Logical; when \code{sample_n} and \code{sample_frac} are both \code{NULL},
automatically infer a tuning sample size from input data size.}

\item{max_candidates}{Optional maximum number of grid points to evaluate.
If exceeded, candidate spacing is expanded automatically. Default is \code{20}.}

\item{reuse_tuned_k}{Logical; when \code{k} is \code{NULL}, tune \code{k} once on baseline and
reuse it across all candidates for speed.}

\item{parallel}{Logical; whether to evaluate candidate grid values in parallel
(POSIX systems only). Default is \code{TRUE}.}

\item{cores}{Number of cores used when \code{parallel = TRUE}.}

\item{seed}{Random seed used for optional sample subsampling.}

\item{object}{Objective used to choose \code{fused_top_v}
(\code{"entropy_elbow"} (default), \code{"diss"}, \code{"silhouette"}, or \code{"eigen"}).}

\item{early_stop}{Logical; when \code{TRUE} and \code{object = "entropy_elbow"},
stop tuning once a stable small-gain elbow is reached. Default is \code{FALSE}
so elbow selection is based on the full evaluated grid.}

\item{elbow_rel_tol}{Relative elbow threshold multiplier on max observed gain.}

\item{elbow_abs_tol}{Absolute lower bound for elbow threshold.}

\item{elbow_min_points}{Minimum evaluated points before early-stop is allowed.}

\item{elbow_min_frac}{Minimum evaluated fraction in \code{v} grid before early-stop.}

\item{elbow_patience}{Number of consecutive small-gain steps required to stop.}

\item{elbow_smooth_window}{Integer running-mean window used to smooth entropy
gains before elbow detection.}
}
\description{
Tune fused weight top-v truncation
}
