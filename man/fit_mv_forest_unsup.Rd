% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_mv_forest.R
\name{fit_mv_forest_unsup}
\alias{fit_mv_forest_unsup}
\title{Fit an unsupervised random forest (native C++ engine)}
\usage{
fit_mv_forest_unsup(
  X,
  ntree = 500L,
  ytry = NULL,
  proximity = c("all", "inbag", "oob", "none"),
  nodesize = 3L,
  max_depth = 0L,
  seed = -1L,
  samptype = c("swor", "swr"),
  nthread = getOption("multiRF.nthread", 0L),
  enhanced_prox = FALSE,
  sibling_gamma = 0.5,
  leaf_embed_dim = 10L
)
}
\arguments{
\item{X}{Data frame or matrix of features (n x p).}

\item{ntree}{Number of trees.}

\item{ytry}{Number of candidate pseudo-Y columns per split. Default \code{NULL} = 15.}

\item{proximity}{Proximity output mode.}

\item{nodesize}{Minimum terminal node size.}

\item{max_depth}{Maximum tree depth (0 = unlimited).}

\item{seed}{Random seed.}

\item{samptype}{Sampling scheme: \code{"swor"} or \code{"swr"}.}

\item{nthread}{Number of OpenMP threads used by the native engine.}

\item{enhanced_prox}{Logical; whether to compute enhanced proximity.}

\item{sibling_gamma}{Strength of the sibling-leaf correction used by
enhanced proximity.}

\item{leaf_embed_dim}{Embedding dimension used by enhanced proximity.}
}
\value{
A list with \code{forest.wt}, \code{proximity}, \code{membership}, \code{xvar},
\code{xvar.names}, \code{ntree}, and \code{engine = "multiRF"}.
}
\description{
Emulates rfsrc unsupervised mode: at each tree, randomly partition the
columns of \code{X} into pseudo-predictor and pseudo-response halves, then fit
a multivariate regression tree.  Only \code{forest.wt} and \code{proximity} are
meaningful; there is no real Y.
}
\keyword{internal}
