% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrf3_reconstr.R
\name{mrf3_reconstr}
\alias{mrf3_reconstr}
\alias{get_reconstr_matrix}
\title{MRF unsupervised clustering -- Reconstruction method}
\usage{
mrf3_reconstr(
  recon = NULL,
  rfit = NULL,
  k = NULL,
  weights_cluster = TRUE,
  dat_use_to_cluster = "ALL",
  model_top_v = 10,
  recon_fusion = c("weighted", "uniform"),
  connection_score = NULL,
  score_power = 1,
  score_floor = 0,
  fallback_uniform = TRUE,
  fused_top_v = NULL,
  fused_row_normalize = TRUE,
  fused_keep_ties = TRUE,
  fusion_mode = "average",
  gamma = 0.1,
  alpha_init = NULL,
  ao_max_iter = 20,
  ao_tol = 1e-04,
  knn_q = NULL,
  hollow = TRUE,
  ao_symm = TRUE,
  ao_verbose = FALSE,
  ...
)

get_reconstr_matrix(
  rfit,
  model_top_v = 10,
  recon_fusion = c("weighted", "uniform"),
  connection_score = NULL,
  score_power = 1,
  score_floor = 0,
  fallback_uniform = TRUE,
  fused_top_v = NULL,
  fused_row_normalize = TRUE,
  fused_keep_ties = TRUE
)
}
\arguments{
\item{recon}{Optional reconstruction object returned by \code{get_reconstr_matrix()}.
If \code{NULL}, reconstruction is built from \code{rfit}.}

\item{rfit}{A model list of random forest models.}

\item{k}{Pre-defined number of clusters. The default is selecting the optimal k by tuning method.}

\item{weights_cluster}{Logical; whether to cluster based on reconstructed weight affinity.}

\item{dat_use_to_cluster}{If clustering method selected as Reconstr, the user can choose the data to conduct clustering.
In weight-clustering mode, shared clustering uses \code{W_all}; this argument only
applies when \code{weights_cluster = FALSE}.}

\item{model_top_v}{Model-level top-v cutoff applied to each single-model
forest weight matrix before fusion.}

\item{recon_fusion}{Reconstruction fusion mode.
\code{"weighted"} (default) uses \code{connection_score}; \code{"uniform"} uses equal weights.}

\item{connection_score}{Optional directional score matrix from \code{find_connection(return_score = TRUE)}.}

\item{score_power}{Exponent applied to raw connection scores before normalization.}

\item{score_floor}{Non-negative floor applied to raw connection scores before normalization.}

\item{fallback_uniform}{Logical; whether to fallback to uniform averaging when weighted scores are unavailable.}

\item{fused_top_v}{Optional integer. If set, apply row-wise top-v truncation to fused weights
(\code{W_all}) after fusion. If \code{FALSE}, no fused top-v truncation is applied.}

\item{fused_row_normalize}{Logical; whether to row-normalize fused weights after optional truncation.}

\item{fused_keep_ties}{Logical; whether fused top-v truncation keeps ties at cutoff.}

\item{fusion_mode}{Clustering-affinity fusion mode.
\code{"average"} keeps current behavior; \code{"ao_reg"} uses alternating optimization with simplex-regularized weights.}

\item{gamma}{L2 regularization strength for the simplex weights in \code{"ao_reg"} mode.}

\item{alpha_init}{Optional initial fusion weights for \code{"ao_reg"} mode.}

\item{ao_max_iter}{Maximum alternating-optimization iterations.}

\item{ao_tol}{Convergence tolerance for \verb{||alpha_t - alpha_\{t-1\}||_1}.}

\item{knn_q}{Optional kNN sparsification per base similarity (rows keep top-q).}

\item{hollow}{Logical; whether to zero out diagonal in base/fused similarities.}

\item{ao_symm}{Logical; whether to symmetrize kNN-sparsified similarities by \code{pmax(S, t(S))}.}

\item{ao_verbose}{Logical; whether to print AO iteration diagnostics.}

\item{...}{Additional arguments passed to clustering backends.}
}
\value{
mrf3 clustering object
}
\description{
MRF unsupervised clustering -- Reconstruction method
}
