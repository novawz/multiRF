% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_t.R
\name{tune_model_top_v}
\alias{tune_model_top_v}
\title{Tune model-level top-v}
\usage{
tune_model_top_v(
  dat.list,
  mod,
  tmin = 10,
  by = 1,
  k = NULL,
  sample_n = NULL,
  sample_frac = NULL,
  auto_sample_n = FALSE,
  max_candidates = 20,
  reuse_tuned_k = TRUE,
  parallel = TRUE,
  cores = NULL,
  seed = 529,
  object = "saturation",
  tau = 0.9
)
}
\arguments{
\item{dat.list}{A list of omics matrices used for clustering.}

\item{mod}{A fitted \code{mrf3} model object.}

\item{tmin}{Minimum model-level top-v cutoff to evaluate.
The effective upper bound is the sample size, allowing the
\code{v >= 0.8 * n} no-truncation rule to be evaluated.}

\item{by}{Base step size for model-level top-v grid construction.}

\item{k}{Optional fixed number of clusters.}

\item{sample_n}{Optional integer. If set, tune on a random subset of samples.}

\item{sample_frac}{Optional fraction in (0, 1]. Used when \code{sample_n} is \code{NULL}.}

\item{auto_sample_n}{Logical; when \code{sample_n} and \code{sample_frac} are both \code{NULL},
automatically infer a tuning sample size from input data size.}

\item{max_candidates}{Optional maximum number of grid points to evaluate.
If exceeded, candidate spacing is expanded automatically. Default is \code{20}.}

\item{reuse_tuned_k}{Logical; when \code{k} is \code{NULL}, tune \code{k} once on baseline and
reuse it across all candidates for speed.}

\item{parallel}{Logical; whether to allow candidate-grid evaluation in fresh
PSOCK worker processes. Parallel evaluation also requires an explicit
\code{cores} value.}

\item{cores}{Number of cores used when \code{parallel = TRUE}. Default \code{NULL}
keeps candidate-grid evaluation serial; supply a positive integer to use
process-level workers.}

\item{seed}{Random seed used for optional sample subsampling.}

\item{object}{Objective used to choose \code{model_top_v}:
\code{"saturation"} (default), \code{"entropy_elbow"}, \code{"diss"}, \code{"silhouette"}, or
\code{"eigen"}. \code{"saturation"} selects the smallest \code{v} whose fused-weight row
entropy reaches a fraction \code{tau} of the no-truncation entropy (linearly
interpolated between grid points), so the choice does not depend on the
grid resolution. \code{"entropy_elbow"} keeps the previous small-gain elbow
heuristic; its elbow is selected among interior grid points, so the
smallest grid candidate cannot be selected directly.}

\item{tau}{Saturation fraction in (0, 1) used by \code{object = "saturation"}.
Default \code{0.9}.}
}
\description{
Tune model-level top-v
}
