% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_mv_forest.R
\name{fit_mv_forest}
\alias{fit_mv_forest}
\title{Fit a multivariate regression forest (native C++ engine)}
\usage{
fit_mv_forest(
  X,
  Y,
  ntree = 500L,
  mtry = NULL,
  ytry = NULL,
  nsplit = 10L,
  proximity = c("all", "inbag", "oob", "none"),
  nodesize = 5L,
  max_depth = 0L,
  seed = -1L,
  samptype = c("swor", "swr"),
  nthread = getOption("multiRF.nthread", 0L),
  enhanced_prox = FALSE,
  sibling_gamma = 0.5,
  leaf_embed_dim = 10L
)
}
\arguments{
\item{X}{Data frame or matrix of predictor features (n x px).}

\item{Y}{Data frame or matrix of response features (n x qy).}

\item{ntree}{Number of trees.}

\item{mtry}{Number of candidate X variables per split. Accepts an integer,
a formula string (\code{"sqrt(p)"}, \code{"p/3"}, \code{"p/2"}), or \code{NULL} for the
default (\code{ceiling(px/3)} for regression, \code{ceiling(sqrt(px))} for
classification). In formulas, \code{p} is the number of predictor columns.}

\item{ytry}{Number of candidate Y variables per split. Accepts an integer,
a formula string (\code{"sqrt(p)"}, \code{"p/3"}), or \code{NULL} for the default
(\code{ceiling(qy/3)}). In formulas, \code{p} is the number of Y columns.}

\item{nsplit}{Number of candidate numeric cutpoints per split variable.}

\item{proximity}{Proximity output mode.}

\item{nodesize}{Minimum terminal node size. Default: 5.}

\item{max_depth}{Maximum tree depth (0 = unlimited). Default: 0.}

\item{seed}{Random seed.}

\item{samptype}{Sampling scheme: \code{"swor"} or \code{"swr"}.}

\item{nthread}{Number of OpenMP threads used by the native engine.}

\item{enhanced_prox}{Logical; whether to compute enhanced proximity.}

\item{sibling_gamma}{Strength of the sibling-leaf correction used by
enhanced proximity.}

\item{leaf_embed_dim}{Embedding dimension used by enhanced proximity.}
}
\value{
A list compatible with rfsrc output, containing:
\describe{
\item{forest.wt}{n x n forest weight matrix}
\item{proximity}{n x n proximity matrix}
\item{membership}{n x ntree terminal node membership}
\item{xvar}{predictor data frame}
\item{yvar}{response data frame}
\item{xvar.names}{character vector of predictor names}
\item{ntree}{number of trees}
\item{tree_info}{per-tree structure for IMD}
}
}
\description{
Drop-in replacement for \code{fit_forest()} when \code{type = "regression"}.
Returns an object with the same interface (\verb{$forest.wt}, \verb{$proximity},
\verb{$membership}, \verb{$xvar}, \verb{$yvar}, \verb{$ntree}, \verb{$xvar.names}) so downstream
code in multiRF works without changes.
}
\keyword{internal}
