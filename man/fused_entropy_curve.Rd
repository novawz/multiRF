% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_t.R
\name{fused_entropy_curve}
\alias{fused_entropy_curve}
\title{Entropy of row-wise top-v truncated weights for every truncation level}
\usage{
fused_entropy_curve(W, keep_ties = TRUE, eps = 1e-12)
}
\arguments{
\item{W}{Square numeric weight matrix (rows are samples).}

\item{keep_ties}{Logical; whether truncation keeps ties at the cutoff, as
\code{truncate_top_v_rows()} does by default.}

\item{eps}{Weights at or below this value are treated as zero.}
}
\value{
A list with \code{v} (integer vector \code{1:ncol(W)}), \code{entropy} (mean
normalized row entropy at each \code{v}), and \code{entropy_inf} (no truncation).
}
\description{
Computes, in closed form, the mean normalized row entropy that
\code{calc_fused_weight_entropy(postprocess_fused_weight(W, top_v = v))} would
return for every \code{v} from 1 to \code{ncol(W)}, plus the no-truncation value.
Each row is sorted once; the entropy of its top-\code{v} renormalized weights is
\code{log(S_v) - T_v / S_v} with \code{S_v} the cumulative weight and \code{T_v} the
cumulative \code{w * log(w)}, so the whole curve costs one sort per row.
}
\keyword{internal}
