% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rfsrc.R
\name{fit_forest}
\alias{fit_forest}
\alias{fit_multi_forest}
\title{Fit random forest model and create model lists}
\usage{
fit_forest(
  X,
  Y = NULL,
  type = "regression",
  nodedepth = NULL,
  ntree = 200,
  forest.wt = "inbag",
  proximity = "all",
  mtry = NULL,
  ytry = NULL,
  nsplit = 10,
  samptype = c("swor", "swr"),
  seed = -10,
  engine = getOption("multiRF.engine", "native"),
  enhanced_prox = FALSE,
  sibling_gamma = 0.5,
  leaf_embed_dim = 10L,
  ...
)

fit_multi_forest(
  dat.list,
  connect_list = NULL,
  var.wt = NULL,
  yprob = 1,
  ytry = NULL,
  seed = -10,
  ...
)
}
\arguments{
\item{X}{A data frame that consider as predictor set}

\item{Y}{A data frame that consider as response set. If Y = NULL, an unsupervised RF is conducted}

\item{type}{Select the type of RF model. The default is regression. Can select from "regression", "classification", and "unsupervised"}

\item{nodedepth}{Maximum depth for the legacy \code{randomForestSRC} fallback.
Ignored by the native engine.}

\item{ntree}{Number of trees.}

\item{forest.wt}{Forest-weight output mode for the legacy
\code{randomForestSRC} fallback. The native engine currently returns an
inbag-style full \verb{n x n} forest-weight matrix regardless of this flag.}

\item{proximity}{Proximity output mode for the legacy \code{randomForestSRC}
fallback. Ignored by the native engine, which always returns the full
\verb{n x n} proximity matrix.}

\item{mtry}{Number of candidate X variables per split. Default \code{NULL} =
\code{floor(sqrt(px))}. Passed to native engine; ignored by rfsrc fallback.}

\item{ytry}{Number of response variables randomly selected per split.
Default \code{NULL} means the native engine uses \code{ceiling(qy/3)}.
Set to a specific integer to override (e.g., \code{ytry = ncol(Y) / 2}).}

\item{nsplit}{Number of candidate numeric cutpoints evaluated per variable.
Native and \code{randomForestSRC} both default to \code{10}; set \code{0} to scan all.}

\item{samptype}{Sampling scheme: \code{"swor"} (without replacement) or \code{"swr"}
(with replacement).}

\item{seed}{Random seed passed to the selected engine.}

\item{engine}{Forest backend. Default is \code{getOption("multiRF.engine", "native")}.
Native is the default and recommended engine. \code{randomForestSRC} is used only
as a non-native fallback when explicitly requested.}

\item{enhanced_prox}{Logical; whether to compute enhanced proximity in the
native engine.}

\item{sibling_gamma}{Strength of the sibling-leaf correction used by
enhanced proximity.}

\item{leaf_embed_dim}{Embedding dimension used by the native enhanced
proximity path.}

\item{...}{Additional arguments passed to \code{randomForestSRC::rfsrc()}
when \code{engine != "native"}.}

\item{dat.list}{A list that contains multi-omics datasets with samples in rows and features in columns. Samples should be matched in each dataset.}

\item{connect_list}{A pre-defined connection list between datasets. If \code{NULL},
all directed pairwise connections are enumerated.}

\item{var.wt}{Optional variable-weight list aligned with \code{dat.list}.}

\item{yprob}{Deprecated. Use \code{ytry} directly instead.}
}
\value{
A model list
}
\description{
Fit random forest model and create model lists
}
\details{
\code{fit_forest()} now defaults to the package-native engine for
classification, multivariate regression, and unsupervised fitting.
\code{randomForestSRC} is optional and is only used when \code{engine != "native"}.
}
