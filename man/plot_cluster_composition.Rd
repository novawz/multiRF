% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cluster_composition.R
\name{plot_cluster_composition}
\alias{plot_cluster_composition}
\title{Plot cluster composition as a heatmap}
\usage{
plot_cluster_composition(
  cluster,
  annotation,
  normalize = c("cluster", "annotation", "total", "none"),
  label = c("count_percent", "count", "percent", "none"),
  na.rm = TRUE,
  drop = TRUE,
  percent_digits = 0L,
  main = "Cluster composition",
  xlab = NULL,
  ylab = "Cluster",
  colours = c("#F7FBFF", "#B4C0E4", "#484878"),
  base_size = 8,
  base_family = "sans"
)
}
\arguments{
\item{cluster}{Cluster labels. A factor preserves its declared level order;
other atomic vectors use first-occurrence order.}

\item{annotation}{Annotation or reference labels paired positionally with
\code{cluster}. Factor levels control the displayed column order.}

\item{normalize}{Normalization direction. \code{"cluster"} gives row fractions,
\code{"annotation"} gives column fractions, \code{"total"} gives fractions of all
retained samples, and \code{"none"} displays raw counts.}

\item{label}{Cell-label style: sample count and percentage, count only,
percentage only, or no label. Percentage labels are unavailable when
\code{normalize = "none"}.}

\item{na.rm}{Logical; if \code{TRUE}, remove pairs with a missing cluster or
annotation. If \code{FALSE}, missing values produce an error.}

\item{drop}{Logical; whether to drop unused factor levels. With
\code{drop = FALSE}, zero-count combinations remain visible.}

\item{percent_digits}{Integer from 0 to 6 giving the number of decimal
places used in percentage labels.}

\item{main, xlab, ylab}{Plot title and axis labels. Use \code{NULL} to suppress a
label.}

\item{colours}{Character vector of at least two colours defining the
sequential fill gradient.}

\item{base_size}{Base font size.}

\item{base_family}{Font family.}
}
\value{
A \code{ggplot} object. Its \code{data} component contains \code{cluster},
\code{annotation}, \code{count}, \code{fraction}, \code{value}, and \code{label}, which can be used
as figure source data. \code{fraction} is \code{NA} when \code{normalize = "none"}.
}
\description{
Build a composition heatmap from paired cluster labels and an external
annotation. The default shows the distribution of annotation classes
within each cluster. Sample labels are paired by position; align them by
sample identifier before calling this function when necessary.
}
\examples{
cluster <- factor(c("C1", "C1", "C1", "C2", "C2"))
subtype <- factor(c("A", "A", "B", "B", "C"), levels = c("A", "B", "C"))
plot_cluster_composition(cluster, subtype)
}
