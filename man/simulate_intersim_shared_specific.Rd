% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_intersim_shared_specific.R
\name{simulate_intersim_shared_specific}
\alias{simulate_intersim_shared_specific}
\title{Simulate Shared + Specific Multi-Omics Data via InterSIM}
\usage{
simulate_intersim_shared_specific(
  n = 500,
  Z_prop = c(0.3, 0.3, 0.4),
  delta_methyl = 2,
  delta_expr = 2,
  delta_protein = 2,
  p_DMP = 0.2,
  p_DEG = NULL,
  p_DEP = NULL,
  p_spec_feat = 0.05,
  delta_spec = c(methyl = 1, gene = 1, protein = 1),
  U_nclass = 2,
  U_within_Z = TRUE,
  max_features_per_block = 400,
  sample_prefix = "S",
  return_workflow_data = TRUE,
  keep_sim_raw = TRUE,
  seed = NULL
)
}
\arguments{
\item{n}{Number of samples.}

\item{Z_prop}{Cluster proportion for InterSIM shared clusters.}

\item{delta_methyl}{Shared-signal strength for methylation in InterSIM.}

\item{delta_expr}{Shared-signal strength for expression in InterSIM.}

\item{delta_protein}{Shared-signal strength for protein in InterSIM.}

\item{p_DMP}{Proportion of differential methylation features in InterSIM.
Default matches \code{InterSIM::InterSIM()} (\code{0.2}).}

\item{p_DEG}{Proportion of differential expression features in InterSIM.
Default matches \code{InterSIM::InterSIM()} (\code{NULL}).}

\item{p_DEP}{Proportion of differential protein features in InterSIM.
Default matches \code{InterSIM::InterSIM()} (\code{NULL}).}

\item{p_spec_feat}{Proportion of features used as specific set \code{T} per omics.}

\item{delta_spec}{Named numeric vector with specific shift size for
\code{methyl}, \code{gene}, and \code{protein}.}

\item{U_nclass}{Number of categories for each specific latent label \code{U}.}

\item{U_within_Z}{Logical; whether each \code{U} is generated within each shared
cluster \code{Z} (recommended).}

\item{max_features_per_block}{Maximum number of features kept per omics block
in returned \code{dat.list} when \code{return_workflow_data = TRUE}.}

\item{sample_prefix}{Prefix for generated sample IDs in \code{dat.list}.}

\item{return_workflow_data}{Logical; whether to return workflow-ready
\code{dat.list}, \code{truth}, and \code{k_ref}.}

\item{keep_sim_raw}{Logical; whether to keep original InterSIM output as
\code{sim_raw}.}

\item{seed}{Optional random seed. If \code{NULL}, current RNG state is used.}
}
\value{
A list containing:
\itemize{
\item \code{X_list}: simulated matrices (\code{methyl}, \code{gene}, \code{protein})
\item \code{Z}: shared labels from InterSIM
\item \code{U}: specific labels per omics
\item \code{S}: shared feature index sets per omics
\item \code{T}: specific feature index sets per omics
\item \code{sim_raw}: raw InterSIM output (optional)
\item \code{dat.list}, \code{truth}, \code{k_ref}: workflow-ready outputs (optional)
}
}
\description{
Generate multi-omics data where shared signal is provided by
\code{InterSIM::InterSIM()} and additional omics-specific signal is injected by
per-omics latent labels \code{U}.
}
\details{
This function wraps the notebook simulation workflow into a reusable package
API and optionally returns workflow-ready \code{dat.list} and truth labels.
}
