% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_sub_mrf.R
\name{fit_sub_mrf}
\alias{fit_sub_mrf}
\title{Fit an ensemble of sub-sampled MRF models for a single connection}
\usage{
fit_sub_mrf(
  X,
  Y,
  n_sub = 15L,
  frac_response = 0.2,
  frac_predictor = 0.2,
  ntree_per_sub = 25L,
  mtry = NULL,
  ytry = NULL,
  min_response = 20L,
  min_predictor = 30L,
  enhanced = FALSE,
  compute_imd = FALSE,
  imd_args = list(),
  seed = 529L,
  parallel = FALSE,
  cores = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X}{Predictor data frame (samples x features) — a single omics block.}

\item{Y}{Response data frame (samples x features) — a single omics block.}

\item{n_sub}{Integer; number of sub-MRF replicates to fit.}

\item{frac_response}{Fraction of response columns to sample per replicate
(default 0.1).  Convergence analysis shows symmetric low fractions
(0.1--0.3 for both response and predictor) yield the best
approximation of the full forest-weight matrix.}

\item{frac_predictor}{Fraction of predictor columns to sample per replicate
(default 0.1).  Avoid setting this to 1.0 when \code{frac_response} is
small, as the asymmetric case leads to poor convergence (each
sub-model overfits to its response subset).}

\item{ntree_per_sub}{Number of trees per sub-MRF (default 50).}

\item{mtry}{Number of candidate X variables per split. Passed through to
\code{fit_forest()}.}

\item{ytry}{Number of candidate Y variables per split. Default \code{0L} means
the C++ engine uses \code{min(qy, ceiling(p/3))}. A positive integer overrides this.}

\item{min_response}{Minimum number of response columns per sub-MRF.}

\item{min_predictor}{Minimum number of predictor columns per sub-MRF.}

\item{enhanced}{Logical; if \code{TRUE}, compute soft enhanced proximity
using full-data sample embeddings within each sub-model.  The result
is stored as \verb{$enhanced_prox} in the output so that
\code{mrf3_cl_prox()} can use it directly without re-traversing trees.
Default \code{FALSE} because the extra computation is non-trivial.}

\item{compute_imd}{Logical; whether to pre-compute IMD weights across sub-models.}

\item{imd_args}{Named list of arguments passed to \code{get_imp_forest()} when
\code{compute_imd = TRUE}.}

\item{seed}{Base random seed.}

\item{parallel}{Logical; if \code{TRUE}, use \code{\link[parallel:mclapply]{parallel::mclapply()}}.}

\item{cores}{Number of cores when \code{parallel = TRUE}.}

\item{verbose}{Logical; print progress messages.}

\item{...}{Additional arguments forwarded to \code{fit_forest()}.}
}
\value{
An rfsrc-like list with:
\describe{
\item{forest.wt}{Averaged n x n forest-weight matrix (all samples).}
\item{forest.wt.oob}{Averaged n x n OOB forest-weight matrix.}
\item{proximity}{Averaged n x n proximity matrix.}
\item{xvar}{Full predictor data (all columns of \code{X}).}
\item{yvar}{Full response data (all columns of \code{Y}).}
\item{ntree}{Total tree count across all sub-MRFs.}
\item{sub_mrf_info}{List with coverage and timing metadata.}
}
}
\description{
When the number of response and/or predictor features is large, fitting a
single MRF with all features is computationally expensive.
\code{fit_sub_mrf()} addresses this by repeatedly sub-sampling features from both
the response and predictor blocks of a \strong{single connection}, fitting a
smaller MRF each time, and averaging the resulting forest-weight and
proximity matrices.
}
\details{
Both \code{forest.wt} and \code{proximity} are n x n matrices that do NOT depend
on feature dimension.  Averaging across sub-models with different feature
subsets produces a smoothed estimate.

\code{forest.wt.oob} is computed from each sub-model's \code{membership} and
\code{inbag} matrices: for sample i, only trees where i is out-of-bag
contribute to its weight row.  This provides a regularized version
suitable for connection selection via \code{find_connection()}. Because this
requires \code{inbag}, sub-MRF currently uses the \code{randomForestSRC} fallback
path even when the package default engine is native.
}
