% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shared_fraction.R
\name{get_shared_frac}
\alias{get_shared_frac}
\title{Compute Shared Fraction Per Omics Block}
\usage{
get_shared_frac(
  dat.list = NULL,
  residual = NULL,
  shared_specific = NULL,
  eps = 1e-12
)
}
\arguments{
\item{dat.list}{Optional named list of original omics matrices (\code{X}).}

\item{residual}{Optional named list of residual matrices (\code{R}).}

\item{shared_specific}{Optional output from \code{get_shared_specific_weights()}.
If provided, \code{residual} and (when needed) reconstructed \code{dat.list} are
extracted from it.}

\item{eps}{Small positive threshold to avoid division by zero.}
}
\value{
A data.frame with one row per omics block:
\code{data}, \code{frob_shared}, \code{frob_specific}, \code{specific_ratio}, \code{shared_frac},
and \code{method}. For backward compatibility, it also includes
\code{frob_data}, \code{frob_residual}, and \code{residual_ratio}.
}
\description{
Shared fraction is computed by the signal-based definition:
\verb{1 - (||R||_F^2 / ||X||_F^2)},
where \code{R = X - X_pred}.
}
